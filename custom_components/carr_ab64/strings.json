{
  "config": {
    "step": {
      "user": {
        "title": "Add an air conditioner",
        "description": "Set up one AC indoor unit connected through an AB64 box and a Modbus TCP↔RTU gateway (e.g. an Elfin EW11). Name it after the AC unit or the room it's in, not the gateway (see below) — host and port are the gateway's own TCP connection, while unit-id below is the AB64 box's own address on that gateway.",
        "data": {
          "name": "Air conditioner name",
          "host": "Gateway host / IP address",
          "port": "Gateway TCP port",
          "unit_id": "AB64 unit-id"
        },
        "data_description": {
          "name": "Sets this AC's entity IDs when they are first created — name it after the AC unit or room, not the gateway.",
          "port": "The TCP port your gateway listens on for Modbus — 502 is the Modbus standard, but check your gateway's own configuration page (an Elfin EW11 defaults to 8899).",
          "unit_id": "The AB64 box's own address on the gateway — unit-id = SW2 × 16 + SW1 + 1 (the vendor's switch table is 1-based, so switches at 0,0 mean 1, not 0). Leave blank to scan 1-64 instead, which takes up to about a minute."
        }
      },
      "select_unit": {
        "title": "Confirm the AB64 unit-id",
        "description": "One or more unit-ids responded on this gateway. If you're not sure which one is this AC, turn it on/off (or change its mode) with the remote and see which entry's state changes to match — the values shown are read live, not cached.",
        "data": {
          "unit_id": "Unit-id"
        }
      },
      "reconfigure": {
        "title": "Reconfigure AB64 connection",
        "description": "Change the gateway's host/TCP port or the AB64 box's own unit-id for this AC unit. The new values are re-verified by reading register 0 before saving.",
        "data": {
          "host": "Gateway host / IP address",
          "port": "Gateway TCP port",
          "unit_id": "AB64 unit-id / DIP switch address"
        },
        "data_description": {
          "port": "The TCP port your gateway listens on for Modbus — 502 is the Modbus standard, but check your gateway's own configuration page (an Elfin EW11 defaults to 8899)."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not open a TCP connection to the gateway. Check host, port, and that the gateway is reachable on the network.",
      "read_timeout": "Connected to the gateway, but the AB64 did not respond on this unit-id. Check wiring, baud rate, and DIP switch address.",
      "no_units_found": "No unit-id in the 1-64 range responded. Check wiring, baud rate, and that the gateway is bridged to the RS-485 bus.",
      "all_units_already_configured": "Every unit-id that responded on this gateway is already set up as another entry. Nothing new to add here.",
      "already_configured": "This host, port, and unit-id combination is already configured."
    },
    "abort": {
      "already_configured": "This host, port, and unit-id combination is already configured.",
      "reconfigure_successful": "AB64 connection updated."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AB64 options",
        "data": {
          "scan_interval": "Poll interval (seconds)",
          "enable_advanced_telemetry": "Enable advanced telemetry sensors"
        },
        "data_description": {
          "scan_interval": "One read takes about 115 ms (measured, not a vendor spec), and this integration reads 2 register blocks per poll by default (3 with advanced telemetry). The default of 5 seconds (1 second is the minimum) uses about 4.6% of the RS-485 bus per unit — 9.2% for 2 units, 18.4% for 4 (6.9/13.8/27.6% with advanced telemetry on). Count every AB64 sharing this gateway, not just this entry, when picking a value.",
          "enable_advanced_telemetry": "Turns on roughly 11 additional sensors for other indoor/outdoor telemetry (coil/outdoor temperatures, compressor current/speed, fan speeds) — indoor air temperature is already included by default. Some of these registers aren't present on every AC model or firmware — those sensors will go unavailable on their own. But if one instead shows a value stuck at 0 that never moves even under heavy load, suspect that register isn't supported on this AC rather than trusting it as a real reading."
        }
      }
    },
    "error": {
      "scan_interval_too_low": "Poll interval must be at least 1 second — this bus may be shared with other AB64 units on the same gateway."
    }
  },
  "entity": {
    "sensor": {
      "error_code": { "name": "Error code" },
      "indoor_temp": { "name": "Indoor temperature (TA)" },
      "indoor_coil_temp_tcj": { "name": "Indoor coil temperature (TCJ)" },
      "indoor_coil_temp_tc2": { "name": "Indoor coil temperature (TC2)" },
      "indoor_fan_rpm": { "name": "Indoor fan speed" },
      "filter_sign_timer": { "name": "Filter sign timer" },
      "outdoor_evaporator_temp_te": { "name": "Outdoor evaporator temperature (TE)" },
      "outdoor_temp_to": { "name": "Outdoor temperature (TO)" },
      "outdoor_discharge_temp_td": { "name": "Outdoor discharge temperature (TD)" },
      "outdoor_suction_temp_ts": { "name": "Outdoor suction temperature (TS)" },
      "compressor_current": { "name": "Compressor current" },
      "compressor_rpm": { "name": "Compressor speed" },
      "lowest_fan_rpm": { "name": "Lowest outdoor fan speed" }
    },
    "binary_sensor": {
      "ab_bus_link": { "name": "AB Bus link" }
    }
  }
}
